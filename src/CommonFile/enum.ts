export enum Modules {
  ADMIN = "ADMIN",
  EMAIL = "EMAIL",
  TWILIO = "TWILIO",
  ESI = "ESI",
}

export enum CollectionNames {
  UserCollection = "UserCollection",
}

export enum Env {
  LOCAL = "LOCAL",
  DEV = "DEV",
}

export enum NLP {
  LUIS = "LUIS",
  QnA = "QnA",
}

export enum WAPIType {
  SendSimpleTextMessage = "SendSimpleTextMessage",
}

export enum ExternalApp {
  WAPI = "WAPI",
}
